# Normalize line endings to LF in the repo and on checkout, so Prettier and the
# CI format check behave identically on Windows, macOS and Linux.
* text=auto eol=lf

# Binary assets must never be line-ending converted.
*.png binary
*.jpg binary
*.pdf binary
*.mp3 binary
*.ttf binary
*.woff binary
*.woff2 binary
