# dependencies & build output
node_modules/
dist/

# OS / editor cruft
.DS_Store
Thumbs.db
*.local

# Source asset packs — kept locally for reference, never shipped. They live in
# art-source/ (OUTSIDE public/) so Vite's build never copies them; the project
# ships only the files it actually references (public/assets/furniture, the
# cyberpunk sheet, public/assets/player/Walk.png). See CREDITS.md for sources.
PortfolioMap/*.zip
art-source/

# Tiled editor session (machine-specific window state); keep the .tiled-project
*.tiled-session

# Local Diablo IV automation scripts — contain a live Discord webhook, not part of the site
scripts view/
